# Check whether a column exist and stop if not

Check whether a column exist and stop if not

## Usage

``` r
check_has_column(data, col, msg = NULL)
```

## Arguments

- data:

  A data frame

- col:

  A column name

- msg:

  A custom error message if the check fails

## Value

boolean Whether the column exists
