# Bind rows of dataframes even if column types differ

Convert columns that differ in type to character, then bind rows.

## Usage

``` r
bind_rows_char(dataframes)
```

## Arguments

- dataframes:

  A list of dataframes

## Value

A list of dataframes with common column types
