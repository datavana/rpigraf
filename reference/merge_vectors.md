# Merge vectors

Merge vectors

## Usage

``` r
merge_vectors(values, default)
```

## Arguments

- values:

  A named vector

- default:

  A second named vector

## Value

The first vector containing all additional named values from the second
vector
