# Get the IRI fragment of an IRI path

Get the IRI fragment of an IRI path

## Usage

``` r
epi_iri_parent(id = NULL, prefix = "~")
```

## Arguments

- id:

  An IRI path

- prefix:

  A prefix added to the IRI fragment, if the ID is not NULL.

## Value

The IRI fragment
