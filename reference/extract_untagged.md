# Function to extract non-tagged text

Function to extract non-tagged text

## Usage

``` r
extract_untagged(xml)
```

## Arguments

- xml:

  The XML as character value.

## Value

A character value where all text contained in tags was stripped.
