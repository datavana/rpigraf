# Get list of all databases

Get list of all databases

## Usage

``` r
db_databases(epi = TRUE)
```

## Arguments

- epi:

  Only keep databases with the epi-prefix and join meta data from the
  main database
