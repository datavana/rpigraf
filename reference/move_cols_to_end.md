# Shift selected columns to the end

Shift selected columns to the end

## Usage

``` r
move_cols_to_end(df, cols)
```

## Arguments

- df:

  A data frame.

- cols:

  A vector of column names

## Value

A dataframe ending with the selected cols if they are present
