# Convert letters to a number, e.g. c becomes 3

Convert letters to a number, e.g. c becomes 3

## Usage

``` r
abc2num(s, base = 26)
```

## Arguments

- s:

  The string to convert (lowercase letters)

- base:

  The number of letters to use (default 26 for a-z)
