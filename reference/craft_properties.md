# Create RAM rows for property data

Create RAM rows for property data

## Usage

``` r
craft_properties(df, cols = c(), fill = c())
```

## Arguments

- df:

  The source data frame

- cols:

  The mapping of source columns to RAM columns

- fill:

  Fixed values

## Value

A RAM-enhanced data frame
