# Create RAM rows for project data

Create RAM rows for project data

## Usage

``` r
craft_projects(df, cols = c(), fill = c())
```

## Arguments

- df:

  The source data frame

- cols:

  The mapping of source columns to RAM columns

- fill:

  Fixed values for the RAM

## Value

RAM rows
