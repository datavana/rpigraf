% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distill.R
\name{extract_segments}
\alias{extract_segments}
\title{Extract segment text and character offsets for a tag id}
\usage{
extract_segments(doc, tagid = NULL)
}
\arguments{
\item{doc}{One of: an \code{xml2::xml_document} already processed by
\code{\link[=annotate_offsets]{annotate_offsets()}} (whose id-bearing elements carry \code{data-start} /
\code{data-end} attributes); a character value containing raw XML, which is
annotated on the fly; or \code{NULL} (e.g. for missing content), in which case
\code{NA} values are returned. Pass a pre-annotated document when extracting
several tag ids from the same content, to avoid re-parsing.}

\item{tagid}{Optional character value giving the tag \code{id} to extract. If
\code{NULL}, all offset-annotated elements are returned.}
}
\value{
A list with elements:
\describe{
\item{segments}{Text pieces joined by \verb{;}.}
\item{offsets}{Ranges as \code{"start-end"} joined by \verb{;}.}
\item{ranges}{A one-element list holding a data frame with columns
\code{id}, \code{tag}, \code{start}, \code{end}, \code{text} (one row per matching element).
The \code{start}/\code{end} columns can be passed directly to
\code{IRanges::IRanges()} for coverage and overlap analysis.}
}
}
\description{
Pulls both the plain text and the character-offset ranges for all elements
matching \code{tagid}. Reads the \code{data-start} / \code{data-end} attributes injected by
\code{\link[=annotate_offsets]{annotate_offsets()}} and works vectorized over the selected node set, so
repeated extraction from the same annotated document is cheap.
}
\details{
Multiple matches (discontinuous annotations sharing an \code{id}) yield multiple
spans, returned in document order so that \code{segments} and \code{offsets} align
element-for-element.
}
\seealso{
\code{\link[=annotate_offsets]{annotate_offsets()}}
}
\keyword{internal}
