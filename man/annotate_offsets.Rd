% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{annotate_offsets}
\alias{annotate_offsets}
\title{Inject character offsets into id-bearing XML elements}
\usage{
annotate_offsets(xml)
}
\arguments{
\item{xml}{Character value containing XML text. May be a fragment with
several top-level nodes.}
}
\value{
An \code{xml2::xml_document} with \code{data-start} and \code{data-end} integer-valued
attributes injected onto all elements that have an \code{id}. Feed the result to
\code{\link[=extract_segments]{extract_segments()}} for a tidy data frame of spans.
}
\description{
Walks the XML tree once in document order, accumulating a running character
offset over all text and CDATA nodes, and stamps \code{data-start} and \code{data-end}
attributes onto every element that carries an \code{id} attribute. Offsets are
1-based and inclusive, and refer to positions in the \emph{plain} (tag-stripped)
text, i.e. the concatenation of all text nodes in document order.
}
\details{
The input is wrapped in a synthetic \verb{<root>} element so that XML fragments
with multiple top-level nodes can be parsed. Mixed content (interleaved text
and elements) is handled correctly: a parent element's span covers both its
text runs and its child elements, while children span only their own content.
Comment and processing-instruction nodes are skipped, consistent with how
\code{\link[xml2:xml_text]{xml2::xml_text()}} and \verb{//text()} treat them.
}
\seealso{
\code{\link[=extract_segments]{extract_segments()}}
}
\keyword{internal}
